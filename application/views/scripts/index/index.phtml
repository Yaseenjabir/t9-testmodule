<?php
  $version = $this->version;
  $line = preg_replace('/^(v\d+\.\d+).*$/', '$1', $version);
?>
<div style="font-family:-apple-system,'Segoe UI',Roboto,sans-serif;padding:28px;max-width:1000px;color:#1a1a2e;">

  <!-- hero -->
  <div style="background:linear-gradient(135deg,#7c3aed,#6d28d9);border-radius:18px;padding:26px 30px;color:#fff;box-shadow:0 14px 44px rgba(124,58,237,.32);display:flex;align-items:center;gap:16px;flex-wrap:wrap;">
    <div style="width:54px;height:54px;border-radius:14px;background:rgba(255,255,255,.18);display:flex;align-items:center;justify-content:center;font-size:26px;">&#128230;</div>
    <div>
      <h1 style="margin:0;font-size:23px;font-weight:800;">Trace9 Test Module</h1>
      <p style="margin:5px 0 0;opacity:.85;font-size:13px;">Deployed by the Patch Manager &mdash; verify install / update / rollback</p>
    </div>
    <div style="margin-left:auto;text-align:right;">
      <div style="font-size:11px;text-transform:uppercase;letter-spacing:.06em;opacity:.85;">Running version</div>
      <div style="font-size:30px;font-weight:800;font-family:monospace;"><?= $this->escape($version) ?></div>
    </div>
  </div>

  <!-- stat cards -->
  <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:20px;">
    <?php
      $cards = [
        ['Status',       'Healthy',      '#16a34a'],
        ['Version line', $line,          '#7c3aed'],
        ['Source',       'GitHub tag',   '#8b5cf6'],
      ];
      foreach ($cards as $c):
    ?>
      <div style="background:#faf8ff;border:1px solid #e4d9fb;border-radius:14px;padding:16px 18px;">
        <div style="font-size:11px;text-transform:uppercase;letter-spacing:.05em;color:#9aa3b2;font-weight:700;"><?= $this->escape($c[0]) ?></div>
        <div style="font-size:18px;font-weight:700;margin-top:6px;color:<?= $c[2] ?>;"><?= $this->escape($c[1]) ?></div>
      </div>
    <?php endforeach ?>
  </div>

  <!-- release notes -->
  <div style="margin-top:20px;background:#faf8ff;border:1px solid #e4d9fb;border-radius:16px;padding:22px 26px;">
    <h2 style="margin:0 0 14px;font-size:15px;font-weight:800;color:#6d28d9;">What&rsquo;s new in <?= $this->escape($line) ?></h2>
    <?php if (empty($this->features)): ?>
      <p style="margin:0;color:#888;font-size:13px;">No release notes for this version.</p>
    <?php else: ?>
      <ul style="margin:0;padding-left:18px;color:#3a3a4e;font-size:14px;line-height:1.9;">
        <?php foreach ($this->features as $f): ?>
          <li><?= $this->escape($f) ?></li>
        <?php endforeach ?>
      </ul>
    <?php endif ?>
  </div>

  <p style="margin:18px 2px 0;font-size:12px;color:#9aa3b2;">testmodule &middot; a throwaway module for exercising the Trace9 Patch Manager flow.</p>
</div>
